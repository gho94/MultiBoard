<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myapp.member.dao.IMemeberRepository">
com.example.myapp.member.model.Member
	<insert id="insertMemeber" parameterType="com.example.myapp.member.model.Member">	
		INSERT INTO member (
		    userid, name, password, email, phone
		) VALUES ( 
		    #{userid}, #{name}, #{password}, #{email}, #{phone}
		)			
	</insert>
	
	<select id="selectMember" parameterType="string" resultType="com.example.myapp.member.model.Member">
		SELECT userid, name, password, phone, email, role
		FROM member
		WHERE userid = #{userid}
	</select>
	
	<select id="selectAllmembers" resultType="com.example.myapp.member.model.Member">
		SELECT userid, name, password, phone, email, role
		FROM member
	</select>

	<update id="updateMember" parameterType="com.example.myapp.member.model.Member">
		UPDATE member
		SET name = #{name}, 
		    password = #{password}, 
		    email = #{email}, 
		    phone = #{phone}
		WHERE userid = #{userid}
	</update>
	
	<delete id="deleteMember", parameterType="com.example.myapp.member.model.Member">
		DELETE FROM member
		WHERE userid = #{userid} AND password = #{password}
	</delete>
	
	<select id="getPassword" resultType="string" parameterType="string">
		SELECT password
		FROM member
		WHERE userid = #{userid}
	</select>
</mapper>